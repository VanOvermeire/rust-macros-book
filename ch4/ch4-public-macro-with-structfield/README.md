# StructField (second implementation)

## Overview

This is the second implementation of the 'public' attribute macro from chapter 4, where we use a custom struct (`StructField`) and implement the `ToTokens` trait.
